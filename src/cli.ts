#!/usr/bin/env node
/**
 * CLI entry point for n8n-backup
 * This file serves as the executable entry point when the package is installed globally
 * or run via bunx/npx
 *
 * @module cli
 */

import { main } from './index.js';

// Execute the main function
main();
